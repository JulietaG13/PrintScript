package edu.handlers.expressions;

import edu.ast.interfaces.ExpressionNode;
import edu.handlers.ExpressionHandler;
import edu.reader.InterpreterReader;

public class LiteralStringHandler implements ExpressionHandler {
  @Override
  public Object handle(ExpressionNode node, InterpreterReader reader) {
    return null;
  }
}
